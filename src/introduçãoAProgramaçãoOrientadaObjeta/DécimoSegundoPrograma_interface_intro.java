package introduçãoAProgramaçãoOrientadaObjeta;
import classes.Jogos;
public class DécimoSegundoPrograma_interface_intro implements Jogos {
	/*
	 * O interface é basicamente um "contrato" na qual nele, é definido
	 * uma série de métodos obrigatórios a ser implementados naquela classe.
	 * Elas são muito úteis para padronizar programas e lidar com métodos abstratos
	 * (falaremos mais sobre abstract depois). O importante é saber que 
	 * os métodos definem um padrão de métodos, que caso não seja seguido,
	 * exibirá erro, como no exemplo abaixo. O nome do método contido na interface
	 * não foi chamado, logo, não seguiu o contrato assinado entre
	 * a interface "Jogos" e a classe deste arquivo. Vou deixar o código
	 * errado propositalmente, caso você deseje, clone esse repositório
	 * e corrija o erro chamando o método iniciar.
	 */

	public static void main(String[] args) {
		
		
		}

	}

	

}
